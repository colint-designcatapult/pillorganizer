import 'package:flutter/widgets.dart';

class MyPillsPage extends StatelessWidget {
  const MyPillsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('My Pills Page'));
  }
}
