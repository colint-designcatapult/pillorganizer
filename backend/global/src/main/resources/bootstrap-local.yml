aws:
  access-key-id: test
  secret-key: test
  services:
    secretsmanager:
      endpoint-override: http://localhost:4566
    dynamodb:
      endpoint-override: http://localhost:4566
      schema: http