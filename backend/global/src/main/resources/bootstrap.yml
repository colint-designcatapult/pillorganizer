micronaut:
  application:
    name: healthe-global
  config-client:
    enabled: true

aws:
  region: ca-central-1
  secretsmanager:
    enabled: true
    secrets:
      - secret-name: database
        prefix: datasources.default